import React from "@rbxts/react";
import Button from "./button";

const App = () => {
    return (
        <screengui>

        <Button onClick={() => print("Clicked!")} />
    </screengui>

        );
};

export = App;
